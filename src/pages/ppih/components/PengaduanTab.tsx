import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export const PengaduanTab = () => {
    return (
      <Card>
        <CardHeader>
          <CardTitle>Layanan Pengaduan</CardTitle>
        </CardHeader>
        <CardContent>
          <p>Daftar tiket pengaduan dan detail tindak lanjut akan ada di sini.</p>
        </CardContent>
      </Card>
    );
  };